const radius = 5;
const area = Math.PI * Math.pow(radius, 2)
const circumference = 2 * Math.PI * radius

console.log("For a circle with a radius of", radius ,"units:" , area)
console.log("The area is approximately", area ,"square units.")
console.log("The circumference is approximately",circumference)